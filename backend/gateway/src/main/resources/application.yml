server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: identity
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/**
        - id: expo
          uri: http://localhost:8082
          predicates:
            - Path=/api/admin/expos/**,/api/exhibitor/**
        - id: payment
          uri: http://localhost:8083
          predicates:
            - Path=/api/exhibitor/payments/**
