server:
  port: 8080
  forward-headers-strategy: framework # nginx가 보낸 X-Forwarded-Proto(https)를 신뢰해서 identity로 넘길 때도 https로 전달함

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: identity
          uri: http://localhost:8081
          predicates:
            - Path=/api/auth/**
        # 소셜 로그인 리다이렉트/콜백 - Spring Security OAuth2 Client 기본 경로
        - id: identity-oauth2
          uri: http://localhost:8081
          predicates:
            - Path=/oauth2/**,/login/oauth2/**
        - id: payment
          uri: http://localhost:8083
          predicates:
              - Path=/api/exhibitor/payments/**,/api/customer/admission-payments/**
        - id: expo-customer
          uri: http://localhost:8082
          predicates:
              - Path=/api/customer/expos/**,/api/customer/consultations/**,/api/customer/vehicles/**
        - id: review
          uri: http://localhost:8085
          predicates:
              - Path=/api/customer/booths/**
        # expo의 "/api/admin/**"보다 먼저 와야 함 - 안 그러면 이 경로가 expo로 잘못 라우팅됨
        - id: reservation
          uri: http://localhost:8084
          predicates:
            - Path=/api/admin/reservation/**,/api/customer/**
        - id: expo
          uri: http://localhost:8082
          predicates:
            - Path=/api/admin/**,/api/exhibitor/**
        # 후기 사진. expo-uploads(/uploads/**)보다 먼저 와야 review로 라우팅된다.
        - id: review-uploads
          uri: http://localhost:8085
          predicates:
            - Path=/uploads/review/**
        # 부스 배너/차량 이미지 등 업로드 파일. <img> 태그는 Authorization 헤더를 못 실어서 화이트리스트 처리됨
        - id: expo-uploads
          uri: http://localhost:8082
          predicates:
            - Path=/uploads/**
        # Swagger UI가 각 서비스 OpenAPI 문서를 게이트웨이 경유로 가져오게 함
        - id: openapi-identity
          uri: http://localhost:8081
          predicates: # 이 경로로 온 요청을 잡아서
            - Path=/v3/api-docs/identity
          filters: # 경로를 이걸로 바꿔서 8081로 보냄
            - SetPath=/v3/api-docs
        - id: openapi-expo
          uri: http://localhost:8082
          predicates:
            - Path=/v3/api-docs/expo
          filters:
            - SetPath=/v3/api-docs
        - id: openapi-payment
          uri: http://localhost:8083
          predicates:
            - Path=/v3/api-docs/payment
          filters:
            - SetPath=/v3/api-docs
        - id: openapi-reservation
          uri: http://localhost:8084
          predicates:
            - Path=/v3/api-docs/reservation
          filters:
            - SetPath=/v3/api-docs
        - id: openapi-review
          uri: http://localhost:8085
          predicates:
            - Path=/v3/api-docs/review
          filters:
            - SetPath=/v3/api-docs
      globalcors: # gateway CORS 설정
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:5173"
              - "http://localhost:5174"
              - "http://localhost:3000"
            allowedMethods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
            allowedHeaders: "*"
            allowCredentials: true

springdoc:
  swagger-ui:
    urls:
      - name: identity
        url: /v3/api-docs/identity
      - name: expo
        url: /v3/api-docs/expo
      - name: payment
        url: /v3/api-docs/payment
      - name: reservation
        url: /v3/api-docs/reservation
      - name: review
        url: /v3/api-docs/review

jwt:
  access-key: ${ACCESS_KEY}
